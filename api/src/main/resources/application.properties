spring.application.name=import-app-api

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/import_app}
spring.datasource.username=${DB_USERNAME:import_app}
spring.datasource.password=${DB_PASSWORD}

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false

spring.flyway.enabled=true
